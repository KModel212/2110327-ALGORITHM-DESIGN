#include <iostream>
#include <vector>

using namespace std;

int main() {
    int n,m;
    cin >>  n >> m;
    vector<int> a(n);
    for(int i = 1 ; i <=n ; i++) {
        cin >> a[i];
    }

    for (int i = 1 ; i<=n ; i ++) {
        for (int j=1 ; j <=m ; j++) {
            if ()
        }
    }
}